{$IFNDEF _TYPES_}
{$DEFINE _TYPES_}


type
   size_t   = dword;
   ssize_t  = dword;
   pid_t    = dword;
   off_t    = dword;
   mode_t   = dword;
   dev_t    = dword;
   ino_t    = dword;
   nlink_t  = dword;
   uid_t    = dword;
   gid_t    = dword;
   time_t   = dword;


{$ENDIF}   { _TYPES_ }
